@model Policy

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="invoice-wrap">
    <div class="page-header">
        <h4 class="text-center mb-30 weight-600">Delete</h4>
        <p>Are you sure you want to delete this?</p>

        <div class="row pb-30">
            <div class="col-md-6">
                <h5 class="mb-15">Name: <span class="font-weight-bold"> @Html.DisplayFor(model => model.Name)</span></h5>
                <h5 class="mb-15">Name: <span class="font-weight-bold"> @Html.DisplayFor(model => model.Company.Name)</span></h5>
            </div>

        </div>
        <div class="invoice-desc pb-30">
            <div class="invoice-desc-head clearfix">
                <div class="invoice-sub">Description</div>
                <div class="invoice-rate">Amount</div>
                <div class="invoice-hours">Emi</div>
            </div>
            <div class="invoice-desc-body">
                <ul>
                    <li class="clearfix">
                        <div class="invoice-sub">@Html.DisplayFor(model => model.Description)</div>
                        <div class="invoice-rate">
                            @Html.DisplayFor(model => model.Amount)
                        </div>
                        <div class="invoice-hours">
                            @Html.DisplayFor(model => model.Emi)
                        </div>
                    </li>
                </ul>
                <form asp-action="Delete">
                    <input type="hidden" asp-for="ID" />
                    <input type="submit" value="Delete" class="btn btn-danger" /> |
                    <a asp-action="Index" class="btn btn-success">Back to List</a>
                </form>
            </div>

        </div>
    </div>
</div>